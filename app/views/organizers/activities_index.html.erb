<div class="container">
<div class="row">
<% @organizer.activities.each do |activity| %>
	<div class="col-md-3">
	  <a class="thumbnail">
	  	  <%= image_tag(activity.img_url) %>
	  </a>  
	  <p>
	    <%= activity.name%>
	  </p>
	</div>
<% end %>