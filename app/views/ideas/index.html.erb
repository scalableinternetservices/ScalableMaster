<div>
  <h4>Ideas:</h4>
  <ul>
    <% @ideas.each do |idea| %>
      <li id="idea-<%= idea.id %>">
        <div><%= idea.title %></div>
        <div><%= idea.description %></div>
      </li>
    <% end %>
  </ul>
</div>