<!DOCTYPE html>
<html lang="en">
<head>
  <title>Gatheracty</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %> 
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %> 
<!--     sets up all the behind-the-scenes data needed to prevent cross-site request forgery attacks, which will be important once we add form -->
  <%= csrf_meta_tags %> 
  <% if notice %>
    <%= notice %>
  <% end %> 
  <% if alert %>
    <%= alert %>
  <% end %> 
</head>

<body class="<%= controller.controller_name%>">
  <div class="page-header" id="header">
    <h1>Gatheracty</h1>
    <% if participant_signed_in? %>
    <%= link_to 'Sign out', destroy_participant_session_path, method: :delete %>
    <% elsif organizer_signed_in? %>
    <%= link_to 'Sign out', destroy_organizer_session_path, method: :delete %>
    <% end %>
  </div>
  <div id="main">
<!--  When we invoke yield on page 44 yield, Rails automatically substitutes in the page-specific content—the stuff
generated by the view invoked by this request.-->
    <%= yield %> 
  </div>
<footer>
  <br>
  <p>Designed by: ScalableMaster</p>
  <br/><br/>
</footer>

</body> 
</html>




