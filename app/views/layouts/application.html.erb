<!DOCTYPE html>
<html lang="en">
<head>
  <title>Gatheracty</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %> 
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %> 
<!--     sets up all the behind-the-scenes data needed to prevent cross-site request forgery attacks, which will be important once we add form -->
  <%= csrf_meta_tags %> 
  <% if notice %>
    <%= notice %>
  <% end %> 
  <% if alert %>
    <%= alert %>
  <% end %> 
</head>

<body class="<%= controller.controller_name%>">
  <div class="page-header" id="header">
    <a class="log" href=
    <% if participant_signed_in? %>
      <%= "/homepage"%>
    <% elsif organizer_signed_in? %>
      <%= "/organizers/#{current_organizer[:id]}/summary" %>
    <% else %>
      <%= "/homepage"%>
    <% end %>
    >
    <h1>Gatheracty</h1></a>
    <div class="row bar">
      <div class="col-xs-3"></div>   

      <div class="col-xs-3">
        <a class="log" href=<%= "/ideas" %>>Ideas</a>
      </div>

      <% if !participant_signed_in? && !organizer_signed_in?%>
        <div class="col-xs-3">
          <%= link_to 'Participant log in', new_participant_session_path, method: :get, :class=>"log" %>
        </div>
        <div class="col-xs-3">
          <%= link_to 'Organizer log in', new_organizer_session_path, method: :get, :class=>"log" %>
        </div>
      <% end %>

      <% if participant_signed_in? %>
        <div class="col-xs-3">
          <a class="log" href=<%= "/participants/#{current_participant[:id]}/ideas/new"%>>Create Idea</a>
        </div>
        <div class="col-xs-3">
          <%= link_to 'Sign out', destroy_participant_session_path, method: :delete, :class=>"log" %>
        </div>
      <% elsif organizer_signed_in? %>
        <div class="col-xs-3">
          <a class="log" href=<%= "/organizers/#{current_organizer[:id]}/activities/new"%>>Create Activity</a>
        </div>
        <div class="col-xs-3">
          <%= link_to 'Sign out', destroy_organizer_session_path, method: :delete, :class=>"log" %>
        </div>
      <% end %>
  </div>
  </div>
  <div id="main">
<!--  When we invoke yield on page 44 yield, Rails automatically substitutes in the page-specific content—the stuff
generated by the view invoked by this request.-->
    <%= yield %> 
  </div>
<footer>
  <br>
  <p>Designed by: ScalableMaster</p>
  <br/><br/>
</footer>

</body> 
</html>




