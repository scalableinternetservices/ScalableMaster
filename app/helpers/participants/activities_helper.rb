module Participants::ActivitiesHelper
	# def cache_key_for_activities(activities, page)
	# 	count = activities.count
	# 	max_updated_at = activities.maximum(:updated_at).try(:utc).try(:to_s, :number)
	# 	"participants-#{page}-activities-#{count}-#{max_updated_at}"
	# end
end
